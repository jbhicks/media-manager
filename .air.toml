
# Config file for Air (https://github.com/cosmtrek/air)

# The root directory to watch for changes.
root = "."
# The directory where the binary will be built.
tmp_dir = "tmp"


[build]
  cmd = "go build -o tmp/media-manager.exe ./cmd/media-manager/main.go 2>&1 | tee tmp/build.log"
  entrypoint = ["./tmp/media-manager.exe"]
  args_bin = ["./media"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["vendor", "media", "bin", "thumbnails", "tmp"]
  delay = 1000
  kill_delay = 500
  stop_on_error = true
  bin = "tmp/media-manager.exe"
  log = "tmp/app.log"

[misc]
  # Delete tmp directory on exit
  clean_on_exit = true